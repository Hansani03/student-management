pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Pulling code from GitHub...'
            }
        }

        stage('Test') {
            steps {
                echo 'Jenkins pipeline is working!'
            }
        }

    }
}